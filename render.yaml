services:
  - type: web
    name: github-repo-tracker
    env: python
    buildCommand: "pip install -r requirements.txt && cd backend/frontend/admin-dashboard && npm install && npm run build && cd ../../../backend && python manage.py collectstatic --noinput && python manage.py migrate"
    startCommand: "cd backend && gunicorn backend.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DJANGO_DEBUG
        value: False
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: "*"
      - key: DATABASE_URL
        value: "postgresql://root:sG0cGFD3r24W84imWPK1JzYXcuz5fUgn@dpg-d3a42mi4d50c73cvrbvg-a.oregon-postgres.render.com/repotracker"